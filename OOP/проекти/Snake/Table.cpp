# include "Table.h"


//void Table::show_table() // функция для вывода таблицы
//{
//	Table t;
//    system("cls");// очищаем консоль
//    for (int i = 0; i <= t.N + 1; ++i)
//     for (int j = 0; j <= t.M + 1; ++j)
//      cout << (i == 0 || j == 0 || i == t.N + 1 || j == t.M + 1 ? '#' : /*  t. */  a[i][j]) << (j <= t.M ? "" : "\n");
//    // выводим таблицу и края
//}