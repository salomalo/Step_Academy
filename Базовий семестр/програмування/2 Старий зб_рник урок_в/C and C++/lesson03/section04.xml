<?xml version="1.0" encoding="windows-1251" ?>
<?xml:stylesheet type="text/xsl" href="common/layout.xsl" ?>

<lesson title="Урок №3" next="hometask.xml" prev="section03.xml" summ="1">
<h2>Примеры к уроку.</h2>
<h3>Пример 1.</h3>
<h5>Постановка задачи.</h5>
Написать программу, которая находит сумму всех целых чисел от 1 до 5 включительно. 
<h5>Код реализации.</h5>
<example>#include &lt;iostream&gt;
using namespace std;
void main(){
	int BEGIN=1; // начало диапазона суммируемых значений
	int END=5; // конец диапазона суммируемых значений
	int SUMM=0; // переменная для накопления суммы
	int i=BEGIN; // управляющая переменная цикла

	// проверка условия 
	while(i&lt;=END){ //(сравнение управляющей переменной с окончанием диапазона)
		SUMM+=i;// накапливание суммы
		i++;// изменение управляющей переменной
	}
	
	// показ результата
	cout&lt;&lt;"Result - "&lt;&lt;SUMM&lt;&lt;"\n\n";
}</example>
<h5>Комментарий к коду.</h5>
<p>В качестве комментария к коду, мы решили представить таблицу, которая досконально описывает каждую итерацию цикла:</p>
<center><img src="images\4.jpg" ></img></center>
<p>При изучении таблицы не трудно заметить, что управляющая переменная, так же выполняет роль переменной последовательно перебирающей значения для суммирования.<tip start="Примечание"> Распространенным заблуждением является то, что управляющая переменная может изменяться только на единицу - это не так. Главное, чтобы переменная изменялась любым логичным образом.</tip></p>
<h3>Пример 2.</h3>
<h5>Постановка задачи.</h5>
<p>Написать программу, выводящую на экран линию из 5 звёздочек.</p>
<h5>Код реализации.</h5>
<example>#include &lt;iostream&gt;
using namespace std;
void main(){
	int COUNT=5; // количество звездочек (длина линии)
	int i=0; // управляющая переменная цикла

	while(i&lt;COUNT){ // проверка условия

		cout&lt;&lt;"*";// вывод звездочки
		i++;// изменение управляющей переменной
	}
	cout&lt;&lt;"\n\n";
}</example>
<h5>Комментарий к коду.</h5>
<p>1. Управляющая переменная на момент проверки условия равна количеству уже нарисованных звездочек. Так происходит потому, что переменная i увеличивается на единицу после каждого вывода *.</p>
<p>2. Цикл остановиться тогда, когда i=5, что будет соответствовать количеству нарисованных *.</p>
<hr/>
<p>Теперь, вам необходимо перейти к самостоятельной работе.Расслабьтесь и получайте удовольствие от выполнения домашнего заданияЖ) Желаем удачи!!!</p>
</lesson>


