<?xml version="1.0" encoding="windows-1251" ?>
<?xml:stylesheet type="text/xsl" href="common/layout.xsl" ?>

<lesson title="Урок №4" next="runtest.xml" prev="section03.xml" summ="1">
<h2>Практические примеры. </h2>
<h3>Пример 1.</h3>
<h5>Постановка задачи.</h5>
<p>
Часы бьют каждый час, столько раз, сколько времени. Написать программу, которая подсчитает, сколько раз пробьют часы за 12 часов.
</p>
<h5>Код реализации.</h5>
<example>
#include &lt;iostream&gt;
using namespace std;
void main(){
	int sum=0;
	for(int bom=1; bom&lt;=12;bom++){
		sum+=bom;// накопление суммы ударов
	}

	// Часы пробили 78 раз.
	cout&lt;&lt;" Hours have punched "&lt;&lt;sum&lt;&lt;"times.\n\n";
}
</example>
<h5>Комментарий к коду.</h5>
<p>1. Изначально объявляется переменная sum равная нулю.</p>
<p>2. Цикл формируется из трех конструкций int bom=1; - начальная инициализация, bom&lt;=12; - условие, bom++ - изменение управляющей переменной.</p>
<p>3. Внутри тела цикла накапливается сумма ударов путем прибавления управляющей переменной к значению общей суммы.</p>
<p>4. Когда i достигнет значения 13, цикл остановится и на экран покажется результат.</p>

<h3>Пример 2.</h3>
<h5>Постановка задачи.</h5>
<p>
Пользователь с клавиатуры последовательно вводит целые числа. Как только пользователь ввел 0, необходимо показать на экран сумму всех введенных чисел.
</p>
<h5>Код реализации.</h5>
<example>
#include &lt;iostream&gt;
using namespace std;
void main(){
	int digit, sum=0;

	for(;;){ // реализация бесконечного цикла

		cout&lt;&lt;"Enter digit:";
		cin&gt;&gt;digit;   // ввод числа
		if(digit==0)        // если введен 0 
			break;      //остановить цикл
		sum+=digit;         // накопление суммы	
	}

	// показ результата
	cout&lt;&lt;" Sum of digits "&lt;&lt;sum&lt;&lt;"\n\n";
}
</example>
<h5>Комментарий к коду.</h5>
<p>1. В программе реализован условно бесконечный цикл. То есть остановка цикла происходит искусственным путем (break).</p>
<p>2. На каждой итерации пользователь вводит число.</p>
<p>3. Осуществляется проверка, если это число - 0, значит пора остановить цикл, если не 0, необходимо прибавить число к общей сумме.</p>
<p>4. После того, как отработает break и цикл прекратит работу, на экран покажется сумма всех введенных с клавиатуры чисел.</p>

<h3>Пример 3.</h3>
<h5>Постановка задачи.</h5>
<p>
Написать программу, которая показывает все числа, которым кратно число, введённое с клавиатуры.
</p>
<h5>Код реализации.</h5>
<example>
#include &lt;iostream&gt;
using namespace std;
void main(){
	int digit;
	cout&lt;&lt;"Enter digit:";
	cin&gt;&gt;digit;

	// цикл перебирает числа от 2 до введенного числа
	for(int i=2;i&lt;digit;i++){
		
		// если число не делится на текущее 
		// значение i без остатка остановить
		// данный шаг и перейти к
		// следующему
		if(digit%i!=0)    
			continue;

		// показать i на экран
		cout&lt;&lt;i&lt;&lt;"\n";	
	}
}
</example>
<h5>Комментарий к коду.</h5>
<p>1. Пользователь вводит число для анализа.</p>
<p>2. Цикл последовательно перебирает все числа от 2 до исходного.</p>
<p>3. Осуществляется проверка: если искомое число на текущее без остатка не делится, необходимо прервать данный шаг цикла и перейти к части i++. (continue).</p>
<p>4. Если искомое число на текущее без остатка делится, то на экран показывается текущее число.</p>
<hr/>
<p>Вот и всё!!! Теперь вы можете приступать к прохождению теста и выполнению домашнего задания. Желаем удачи!!!</p>
</lesson>
