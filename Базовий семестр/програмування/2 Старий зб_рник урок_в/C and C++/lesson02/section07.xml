<?xml version="1.0" encoding="windows-1251" ?>
<?xml:stylesheet type="text/xsl" href="common/layout.xsl" ?>

<lesson title="Урок №2" next="section08.xml" prev="section06.xml" summ="1">
<h2>Практический пример на принадлежность точки кольцу.</h2>
<h3>Постановка задачи</h3>
<table>
<tr>
<td>
<p>На плоскости нарисовано кольцо с центром в точке (x0,y0). И радиусами границ r1&lt;r2. Кроме того, на этой же плоскости дана точка с координатами (x, y). Необходимо определить, принадлежит ли эта точка кольцу.</p>
</td>
<td>
<img src="images\Circle.png"/>
</td>
</tr>
</table>
<h3>Решение задачи</h3>
<table>
<tr>
<td>
<p>Для решения задачи необходимо вычислить расстояние от центра кольца до точки, и сравнить его с радиусами:<br/>1. Если длина отрезка от центра до точки меньше чем радиус внешней окружности и при этом больше, чем радиус внутренней окружности, то точка принадлежит кольцу.
<example>if(L&gt;=r1&amp;&amp;L&lt;=r2)</example>	
В противном случае точка не принадлежит кольцу.<br/> 
2. Для выяснения расстояния, от центра до точки мы воспользуемся теоремой Пифагора - <b>Квадрат гипотенузы равен сумме квадратов катетов.</b> Следовательно - <b>длина гипотенузы равна корню квадратному из суммы квадратов катетов.</b> 
<tip start="Примечание">Нам понадобятся дополнительные знания для получения степени и корня квадратного.<br/>
1. В программу необходимо подключить библиотеку для использования математических функций под названием math.h.<br/>
2. Для возведения в степень используется функция pow(double num,double exp), где num это число для возведения в степень, а exp - сама степень.<br/>
3. Для извлечения корня квадратного используется функция sqrt(double num), где num это число из которого извлекают корень.
</tip>
<example>
c=sqrt(pow(a,2)+pow(b,2));
L=c;
</example>
3. Осталось выяснить длины катетов, на рисунке видно, как это сделать. 
<example>
a=y-y0;
b=x-x0;
</example>
Теперь осталось собрать все части решения в единое целое.
</p>
</td>
<td>
<img src="images\Circle2.png"/>
</td>
</tr>
</table>
<h3>Код реализации.</h3>
<example>
#include &lt;iostream&gt;
#include &lt;math.h&gt;
using namespace std;
void main() {
  	// Объявление переменных
  	int x0, y0, r1, r2, x, y;
  	float L;

 	 // Запрос на ввод необходимых данных
	cout&lt;&lt;"Input coordinates of circle’s center (X0, Y0): ";
  	cin&gt;&gt;x0&gt;&gt;y0;
  	cout&lt;&lt;"Input circle radiuses R1 and R2: ";
  	cin&gt;&gt;r1&gt;&gt;r2;
  	cout&lt;&lt;"Input point coordinates (X, Y): ";
  	cin&gt;&gt;x&gt;&gt;y;
	
	// Выведение формулы
  	L = sqrt(pow(x - x0, 2) + pow(y - y0, 2));

	//Анализ результатов
  	if ((r1 &lt; L)&amp;&amp; (L &lt; r2 )) {
    		cout&lt;&lt;"This point is situated inside the circle.\n";
  	}
 	else {
    		cout&lt;&lt;"This point is not situated inside the circle.\n";
  	}
}
</example>
</lesson>
